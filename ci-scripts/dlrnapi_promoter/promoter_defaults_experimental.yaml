# Defaults configuration for experimental features of the promoter
overcloud_images:
  qcow_servers:
    rdo:
      host: "images.rdoproject.org"
      user: "uploader"
      root: "/var/www/html/images/"
    private:
      host: "38.145.34.141"
      root: "/var/www/rcm-guest/images/"
      user: "centos"
    staging:
      host: "localhost"
      root: "/tmp/promoter-staging/overcloud_images"
      user: null
  qcow_images:
    - ironic-python-agent.tar
    - ironic-python-agent.tar.md5
    - overcloud-full.tar
    - overcloud-full.tar.md5
    - undercloud.qcow2
    - undercloud.qcow2.md5
registries:
  source:
    name: local-source-registry
    port: 6000
    host: localhost
    schema: v2_s2
    secure: False
  targets:
    - name: local-target-registry1
      port: 6500
      host: localhost
      secure: False
      schema: v2_s2
#    - name: local-target-registry2
#      port: 6550
#      host: localhost
#      secure: True
#      schema: v2_s2
#      username: ciuser
#      password: test1
