inventory_dir: "{{ ansible_env.HOME  }}/inventory"
inventory_file: "{{ inventory_dir}}/inventory.ini"
keys_dir: "{{ ansible_env.HOME }}/keys"
infra_setup_repo: git_repo
infra_setup_dir: "{{ ansible_env.HOME  }}/infra-setup"
bin_dir: "{{ ansible_env.HOME  }}/bin"
venv_dir: "{{ ansible_env.HOME  }}/venv"

