- hosts: all
  vars:
    promoter_user: zuul
  tasks:
    - name: Run promotion server provisioning role
      include_role:
        name: ci-scripts/infra-setup/roles/promoter
      vars:
        setup_staging: true

