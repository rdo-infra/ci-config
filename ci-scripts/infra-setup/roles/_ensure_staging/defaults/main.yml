# NOTE if you are changing promoter user and promoter_virtualenv
# here please make sure that it also get changed in
# playbooks/mol/pre.yml file.
promoter_user: promoer
ci_config_remote_src_dir: "~/ci-config"
staging_action: setup
staging_args: "--scene dlrn,overcloud_images"
promoter_virtualenv: promoter_venv
