---
# {{ ansible_managed }}
# TODO: Keep in sync with ci-scripts/infra-setup/tenant_vars/*/servers.yml
all:
  vars:
    cloud: "{{ cloud | mandatory }}"
  children:
    {{ cloud | mandatory }}:
      hosts:
        incockpit:
        promoter:
        provisioner:
        rrcockpit:
        toolbox:
