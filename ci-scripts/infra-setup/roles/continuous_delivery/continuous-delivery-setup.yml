configuration-playbook:

yum:
    name: virtualenv

- name: prepare venv ansible-pull
  pip:
    name: ansible
    virtualenv: ~/ansible/
    virtualenv_python: python2.7
  with_items:
    - ansible
    - shade

