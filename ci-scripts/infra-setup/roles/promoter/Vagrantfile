# Used only to allow local development testing of molecule scenarios
#
# Start it with `vagrant up` and molecule will be able to find the test host.
#
Vagrant.configure("2") do |config|
    config.vm.box = "centos/7"
    config.vm.hostname = "tester"
