container_images_template:

- imagename: "{{namespace}}/{{name_prefix}}hello-world{{name_suffix}}:{{tag}}"
  image_source: kolla
  params:
  - ContainerAodhApiImage
  services:
  - OS::TripleO::Services::AodhApi

