---
- hosts: all
  tasks:
    - name: common cleanup tasks
      include_tasks: ../../container-push/cleanup.yml
      tags:
        - registry_cleanup
         # Only the last play should have the registry_cleanup set to work with
         # tripleo-common-integration test
