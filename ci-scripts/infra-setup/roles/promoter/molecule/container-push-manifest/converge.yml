---
- name: Download and repush container and multi-arch images on insecure registries
  hosts: all
  tasks:
    - name: Test promoter container push
      block:
        - name: Include common converge
          include_tasks: ../../container-push/converge.yml
