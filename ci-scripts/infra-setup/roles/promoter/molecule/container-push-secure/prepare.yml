---
- name: Download and repush container images to secure registries without multi-arch manifests
  hosts: all
  vars:
    stage_config_args: "--stage-config-file stage-config-secure.yaml"
  tasks:
    - include_tasks: ../container-push/prepare-common.yml
