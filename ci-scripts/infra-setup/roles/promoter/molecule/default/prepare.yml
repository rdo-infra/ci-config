---
- hosts: all
  tasks:
      #    - name: update packages # noqa 403
      #      become: yes
      #      yum:
      #       name: '*'
      #       state: latest

    - name: disable getty
      systemd:
        name: getty-tty1
        state: stopped
        enabled: false

    - name: disable getty
      systemd:
        name: serial-getty
        state: stopped
        enabled: false

    - name: install development tools
      become: yes
      yum:
        name: '@Development tools'
        state: present
