security_groups:
    - name: sec_group_from_jumphost_external
      rules:
        - protocol: tcp
          port_range_min: 22
          port_range_max: 22
          remote_ip_prefix: "{{ jumphost_floating_ip }}"
keypairs:
    - name: "{{ default_keypair_name  }}"
      public_key_file: rdo-tripleo-ci-infra.pub
