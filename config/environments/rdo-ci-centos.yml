# rdo specific config for container jobs
#
environment: rdo-ci-centos

overcloud_containers_custom_config: overcloud_containers_rdoproject.yaml.j2
overcloud_containers_config: /tmp/overcloud_containers.yaml
docker_registry_namespace: tripleo

