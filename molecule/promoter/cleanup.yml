---
- hosts: instance
  tasks:
    # TODO(rfolco): cleanup artifacts left by controller
    # and stuff in the remote instance - idempotency
    - name: Cleanup provisioning
      debug:
        msg: Not implemented
