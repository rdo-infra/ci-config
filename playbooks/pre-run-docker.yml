---
- hosts: all
  vars:
    docker_group: docker
    promoter_user: "promoter"
  tasks:

    # until docker-install role on redhat distros is fixed by:
    # https://review.openstack.org/#/c/634934

    - name: "Include docker pre-run tasks"
      include_tasks: 'run-docker.yml'
