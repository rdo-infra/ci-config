- hosts: all
  tasks:

    # we just need to install docker from here
    - name: install docker
      package:
        name: docker
        state: present
      #    - name: Run promotion server provisioning role
      #      include_role:
      #        name: promoter
      #      vars:
      #        setup_staging: true

    - name: Run stagig setup parts
      include_role:
        name: promoter
        tasks_from: setup_staging_pre
      vars:
        setup_staging: true
    - name: Run stagig setup parts
      include_role:
        name: promoter
        tasks_from: setup_staging
      vars:
        setup_staging: true


    - name: Stage promotion
      include_role:
        name: stage_promotion
