- hosts: all
  tasks:
    - name: Run promotion server provisioning role
      include_role:
        name: promoter
      vars:
        setup_staging: true

    - name: Stage promotion
      include_role:
        name: stage_promotion
