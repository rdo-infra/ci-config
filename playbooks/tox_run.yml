---
- hosts: all
  tasks:
    - name: Install GSSAPI required packages
      become: true
      ansible.builtin.package:
        name:
          - python3-devel
          - krb5-devel
          - gcc
        state: present

    - import_role:
      name: tox
