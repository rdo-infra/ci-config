---
- hosts: all
  tasks:
    - name: Running molecule
      # TODO(ssbarnea): remove --debug once we have it running for a longer
      # period of time. Without --debug is impossible to guess why create
      # may have failed.
      command: tox --debug -e molecule
      args:
        chdir: "{{ zuul.project.src_dir }}"
